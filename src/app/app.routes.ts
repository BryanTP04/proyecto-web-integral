import { Routes } from '@angular/router';
import { ProductosComponent } from './productos/productos/productos.component'; // ajusta la ruta si es diferente

export const routes: Routes = [
  {
    path: 'productos',
    component: ProductosComponent
  }
];
